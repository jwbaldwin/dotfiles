# Slack drafting examples

These are fictional examples for calibration, not quotes or fixed templates. Match the current conversation and supported facts

## Review request

> @teammate mind giving this a review? [MR]
>
> Fixes the missing error message so we can tell which request failed

## Several changes

> A couple MRs here:
> - [MR] moves the repeated checks into one place
> - [MR] removes the extra lookup when we already have the result
>
> keen on your thoughts!

## Constructive disagreement

> I like having this visible! I think the commit hashes make it hard to scan though. Could we show the MR titles and tag the people who need to take a look?

## Reply in an established thread

> Ah nice, that explains it. Will try again with the new version!

## Technical explanation

> The retry passes, but the dashboard still shows the old failure because it only replaces results with the same test name. The setup error and actual test use different names, so both stay visible

The promise in the reply requires an actual intention to retry. Praise and exclamation marks are optional. Add detail when the reader needs the mechanism; a familiar thread may need only one line
